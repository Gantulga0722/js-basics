function findSec(x, y) {
  let min = x * 60;
  let sec = min + y;
  console.log("Second:", sec);
}
findSec(3, 4);

 // if ((min = 60)) {
  //   min = 0;
  // } else if (min < 60) {
  //   min = min;
  // } else if (min > 60) {
  //   min = min - 60;
  // }